<div class="flex justify-center mt-48 mb-48 container mx-auto">
    <div class="w-3/5 bg-white shadow-lg p-10">
        <h1 class="section-title flex justify-between">
        <%= @inventory.name %>
         <%= button_to "Add food", new_inventory_food_path, method: :get, class: 'btn' %>
        </h1>

       

        <table>
        <tr>
            <th>Food</th>
            <th>Quantity</th>
            <th>Actions</th>
        </tr>
        <% @inventory.inventory_foods.includes(:food).each do |invfoo| %>
            <tr>
                <td> <%= invfoo.food.name %> </td>
                <td> <%= invfoo.quantity %> </td>
                <td>
                    <%= button_to "Remove", food_path(invfoo.food.id), method: :delete %>
                </td>
            </tr>
        <% end %>
        </table>
    </div>
</div>